<resources>
    <!-- Default screen margins, per the Android Design guidelines. -->
    <dimen name="activity_horizontal_margin">16dp</dimen>
    <dimen name="activity_vertical_margin">16dp</dimen>

    <!-- 앱 전체적으로 쓰이는 패딩 -->
    <dimen name="padding_outer">6dp</dimen>
    <dimen name="padding_inner">3dp</dimen>
    <dimen name="margin_outer">6dp</dimen>
    <dimen name="margin_inner">3dp</dimen>

    <!-- 버튼레이아웃은 outer 패딩의 2배 -->
    <dimen name="padding_main_button_layout">12dp</dimen>
    <dimen name="margin_main_button_layout">12dp</dimen>
    <dimen name="main_button_layout_height">48dp</dimen>

    <!-- Admob -->
    <dimen name="main_admob_layout_height">50dp</dimen>

    <!-- Widget Window, 원래 높이 240(갤3 기준) -->
    <!-- 240에서 4 증가 후 나누기 2 -->
    <dimen name="main_widget_window_layout_height">240dp</dimen>

</resources>
